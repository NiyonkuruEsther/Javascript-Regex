// the below line means that it will match what is in the brackets regarding the order they are in
const reg = /[abc]/gm;

// the below line means that if one has written a or b or c then followed by b it will match
// Note the or in the above explanation
const regex = /[abc][b]/gm;


